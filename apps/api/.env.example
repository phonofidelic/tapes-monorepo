# apps/api — copy to .env.local and fill in as needed.
# See apps/api/README.md.

# 'development' enables the HTTPS/cert path (reads localhost-key.pem + localhost.pem).
NODE_ENV=development

# Port the sync server listens on. Defaults to 3031 if unset.
PORT=3031
