# @tapes-monorepo/tailwind-config

Shared Tailwind theme for [Tapes](../../README.md). Exposes `./theme.css`, which
apps and packages import so styling stays consistent across the monorepo.

```css
@import '@tapes-monorepo/tailwind-config/theme.css';
```
